from hashlib import new
import os
from pathlib import Path
import json

def change_saved_script(address, frequency, index_name, attributes, script_name):

    print(address)
    print(frequency)
    print(index_name)
    print(attributes)
    print(script_name)

    return None
